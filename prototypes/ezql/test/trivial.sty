temp_readings = stream (:room_id, :temperature);

//hot = temp_readings[3 sec]
//        .where(t -> t.temperature > 30)
//        .select(t -> { :temp = t.temperature, :blah = 3 + 5 * 6 });

//currentTemp = tempreadings.asContValue(:temperature).sum()[3 sec].sum();

currentTemp = temp_readings[3 sec].sum(:temperature);
