stocks = new Stream(symbol, price)

class stream =
  filter(fn) =
    match stream with
      | [] -> []
      | (x:xs) -> if fn(x)
                    then x : xs.filter(fn)
                    else xs.filter(fn)

stocks.filter(fun e -> e.price > 100) |> puts