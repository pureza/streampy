- Create a stream.Partition method to avoid doing stream.Where(predicate) and stream.Where(not predicate)
- ContValueWindow.Previous?
- Testar o + nos COntValueWindow
- Analisar os ev.["value"]

TESTAR:

- Operações de cv's
  - cv + int -> cv
  - cv + cv -> cv
  - cv[] + int ->
  - cv[] + cv
  - cv[] + cv[]
  - cv && cv
  - cv[] && cv
  - cv[] && cv[]
  - cv.sum()
  - cv[].sum()
  - fn(cv)
  - fn(cv[])
  